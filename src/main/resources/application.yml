server:
  port: 8090

spring:
  application:
    name: spring-ai-demo
  ai:
    zhipuai:
      api-key: ${替换成你自己的key}
      chat:
        options:
          temperature: 0.7
          model: glm-3-turbo
        enabled: true